###############################################################################
# CICA-GPRO — Docker avec MySQL + phpMyAdmin
#
# NE PAS COPIER ce fichier vers .env !
# Ce fichier est utilise UNIQUEMENT par docker-compose.
#
# ⚠️  CHANGEZ TOUS LES MOTS DE PASSE AVANT DE DEPLOYER EN PRODUCTION !
#
# Usage :
#   docker compose --env-file .env.docker.mysql up -d --build
#
# Identifiants par defaut :
#   MySQL User  : gpro / CHANGEZ_MOI
#   MySQL Root  : root / CHANGEZ_MOI
#   phpMyAdmin  : http://localhost:8081
#   Application : http://localhost:8090
###############################################################################

# ── Application ──
APP_NAME=CICA-GPRO
APP_ENV=production
APP_DEBUG=false
APP_URL=http://localhost:8090
APP_PORT=8090
GPRO_MODE=selfhosted

# ── Base de donnees MySQL ──
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=cica_gpro
DB_USERNAME=gpro
DB_PASSWORD=CHANGEZ_MOI
DB_ROOT_PASSWORD=CHANGEZ_MOI_AUSSI

# ── Redis ──
REDIS_HOST=redis
REDIS_PASSWORD=CHANGEZ_MOI_REDIS
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis

# ── Email (log par defaut, configurer SMTP en prod) ──
MAIL_MAILER=log

# ── Broadcast (log = desactive) ──
BROADCAST_DRIVER=log

# ── phpMyAdmin ──
PHPMYADMIN_PORT=8081
