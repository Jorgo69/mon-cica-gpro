Mets à jour la documentation suite aux changements récents.

1. Repère les fichiers modifiés dans la session courante.
2. Identifie la doc impactée : README.md, commentaires de fonctions publiques, fichiers `docs/` si présents.
3. Propose les mises à jour nécessaires (diffs, pas de réécriture complète).
4. Si une nouvelle décision d'archi est apparue, propose une entrée pour `DECISIONS.md`.
5. Si un nouveau pattern de code a émergé, propose une entrée pour `PATTERNS.md`.

ATTENDS validation avant d'écrire. Pas de doc verbeuse : clair, court, utile.
